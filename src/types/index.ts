export interface IComment {
  name: string;
  special: string;
  content: string;
  avatar: string;
}
export interface IPeople {
  name: string;
  position: string;
  description: string;
  avatar: string;
}
